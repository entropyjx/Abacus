# Java Functions
```
//Array Length
int exampleArraySize = exampleArray.length;

// New array
int[] nums = new int[] { -1, -1 };

//Min and Max values
Integer.MAX_VALUE;
Integer.MIN_VALUE;

//Foreach
for (type var : array) 
{ 
    statements using var;
}

//CharAt
str.charAt(3)

//String length
str.length()

//Hashmap
 // Creating an empty HashMap
Map<Integer, String> hash_map = new HashMap<Integer, String>();

//Map contains
hash_map.containsKey(12);

//Converting a string to characters and sorting characters
//Converting character array to string
char[] ca = s.toCharArray();
//This is an IN PLACE sort, does not return
Arrays.sort(ca);
String key = String.valueOf(ca);

//Stringbuilder
StringBuilder sb = new StringBuilder("");
sb.append("hey");
//clearing
sb.setLength(0);
```

